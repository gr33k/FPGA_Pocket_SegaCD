# Genesis runtime smoke debug plan

Historical note: this plan was created while the renamed package was still black-screening. The final hardware control tests proved the black screen was caused by stale and duplicate SD package state, not by the bitstream or runtime design.

Current status:
- obsolete as an active blocker for Genesis cartridge mode
- retained as historical debugging context only
