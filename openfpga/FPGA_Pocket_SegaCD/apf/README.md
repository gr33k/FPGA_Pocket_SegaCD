# APF package-side skeleton for Genesis-only build

This folder is intentionally placeholder-only for now.

It should eventually contain the APF payload copy needed for release packaging:

- `core.json`
- `data.json`
- `input.json`
- `video.json`
- `audio.json`
- `interact.json`
- `variants.json`

For this milestone, these files remain sourced from `apf/` at repo root.

