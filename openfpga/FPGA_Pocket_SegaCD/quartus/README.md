# Quartus skeleton directory for openFPGA package prep

This directory is intentionally placeholder-only. It is reserved for:

- `qpf` / `qsf` / `sdc` / constraint files
- generated synthesis artifacts (not committed in this task)
- future project integration checks

Quartus outputs are intentionally absent; this directory is a packaging and
project hygiene scaffold only.

